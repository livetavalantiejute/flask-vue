{
  "name": "flask-vue",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "waitress-serve --port=$PORT backend.main:app",
    "build": "cd frontend/frontend && npm run build"
  }
}
